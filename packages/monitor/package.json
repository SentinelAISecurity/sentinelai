{
  "name": "@sentinelai/monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Blockchain contract monitoring and alerting service",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@sentinelai/shared": "workspace:*",
    "@stellar/stellar-sdk": "^12.0.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "eslint": "^9.8.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.4"
  }
}

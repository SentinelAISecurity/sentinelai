{
  "name": "@sentinelai/scanners",
  "version": "1.0.0",
  "private": true,
  "description": "Smart contract vulnerability scanner framework with plugin support",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint . --ext .ts",
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@sentinelai/shared": "workspace:*",
    "solc": "0.8.25"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.4"
  }
}
